# Fix Map Cover Images and Deletion Bugs

## What was fixed

### 1. "Photo ไม่ขึ้นเวลาเปิดดู VIEW DETAILS หรือ World Map"
**สาเหตุ:** ระบบเซฟรูปภาพปกและรูปพื้นหลังที่ผู้ใช้ครอบตัด (Crop) เป็นรูปแบบ Data URL (base64 string) ไว้ในฐานข้อมูล ซึ่งกินพื้นที่สูงมาก ส่งผลให้ระบบลบรูปออกอัตโนมัติเมื่อมีการโหลดแอปพลิเคชันซ้ำ เพื่อป้องกันปัญหา Storage Quota Exceeded ของ LocalStorage ทำให้รูปหายไปเมื่อกดดูแผนที่
**วิธีแก้ไข:** เพิ่มระบบอัปโหลดอัตโนมัติในขั้นตอน `publishMapToCommunity` (และการกด Publish จาก Modal) เมื่อระบบพบว่ารูปภาพเป็น Data URL จะทำการแปลงเป็นไฟล์และอัปโหลดขึ้น Supabase Storage โดยอัตโนมัติ จากนั้นจะแทนที่ด้วยลิงก์ URL จริง (https://...) ก่อนเซฟลงฐานข้อมูล ทำให้รูปภาพอยู่ถาวรและไม่หายไปอีกต่อไป ทั้งปกในหน้ารายละเอียดและพื้นหลังใน World Map

### 2. "ลบรูปตอน Edit Map แล้วแต่รูปยังค้างอยู่ (ถูกเพิ่มกลับเข้ามาใหม่)"
**สาเหตุ:** ในขั้นตอนการเซฟข้อมูลอัปเดตแอปมีโค้ดตรวจสอบ fallback เป็น `updates.imageUrl || editorSetup?.imageUrl || null` ซึ่งหมายความว่าถ้าผู้ใช้ลบรูป (ทำให้รูปปัจจุบันมีค่าว่างหรือ null) ระบบจะมองว่าค่าว่างนั้นเป็น `false` แล้วเด้งกลับไปดึงรูปเก่าจาก `editorSetup` มาใส่แทน ทำให้รูปที่ลบไปกลับมาอีกครั้ง
**วิธีแก้ไข:** แก้ไขเงื่อนไขในไฟล์ `MapEditor.jsx` เป็น `updates.imageUrl !== undefined ? (updates.imageUrl || null) : (editorSetup?.imageUrl || null)` เพื่อบังคับให้ระบบสนใจเฉพาะการตั้งใจแก้ข้อมูลของผู้ใช้ หากผู้ใช้ลบรูปจริงๆ จะเก็บค่า null ให้ทันทีแบบ Real-time โดยไม่ดึงรูปเก่ากลับมาอีก
